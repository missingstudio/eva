Write one conventional commit message for the staged diff below.

Answer with JSON only, in this shape and nothing else:

{"message": "type(scope): description"}

Use one type from feat, fix, refactor, chore, docs, test, ci or build. The
scope is optional. Lowercase, imperative mood, no period at the end.

The staged diff:

{{diff}}
