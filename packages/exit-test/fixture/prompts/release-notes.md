Write short release notes from the highlights below. Two or three sentences
of plain prose, addressed to a person who uses the software but did not
build it.

Answer with JSON only, in this shape and nothing else:

{"notes": "the release notes as one string"}

The highlights, as JSON:

{{highlights}}
