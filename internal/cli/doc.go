// Package cli is the process surface and the assembly: what a command line may
// say, what being asked to stop means, the Provider and sink and Session a run
// is built from, and the frontend that is then handed the whole of it.
package cli
