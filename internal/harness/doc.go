// Package harness is the layer that owns answering a prompt: the contract a
// Harness satisfies, the registry that selects one by name, and the assembly
// that opens a Run on a Session and commits what a Harness emits.
package harness
