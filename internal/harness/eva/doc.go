// Package eva is Eva's own Harness, and one entry in the registry that selects
// one. It holds the Loop: the Unit that answers a prompt by assembling one
// call, replaying what the Provider yields into the Trace, and closing the Run
// with the claim of how it went.
package eva
